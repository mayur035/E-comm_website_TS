import { createSlice } from "@reduxjs/toolkit";

const initialState={filterProducts:{}}
const ProductCart = createSlice({
    name:'ProductCart',
    initialState,
    reducers:{

    }
})

export default ProductCart.reducer
// export const {} = ProductCart.actions